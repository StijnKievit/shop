<div class="field">
    <div class="control">
        <button class="button {{isset($options['class']) ? $options['class'] : ''}}" type="{{$type}}" value="{{$value}}" >{{$label}}</button>
    </div>
</div>